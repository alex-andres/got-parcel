import React from 'react';
import ActionCardContainer from '../components/Manager/ActionCardContainer';

const Manager = ({ iconTitles }) => {
	return (
		<ActionCardContainer iconTitles={iconTitles} />
	);
};

export default Manager;